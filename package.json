{
  "name": "ws-nats",
  "version": "1.0.0",
  "description": "WebSocket NATS client",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "test": "tsc && nyc ava --verbose -T 5000",
    "cover": "nyc report --reporter=html && open coverage/index.html",
    "debugtest": "tsc && node $NODE_DEBUG_OPTION node_modules/.bin/ava --verbose -T 6500000 -m"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com//aricart/ws-nats"
  },
  "keywords": [
    "NATS",
    "websockets"
  ],
  "author": "Alberto Ricart",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^10.1.2",
    "@types/ws": "^5.1.1",
    "ava": "1.0.0-beta.4",
    "browser-env": "^3.2.5",
    "jsdom": "^11.10.0",
    "nyc": "^11.8.0",
    "ts-loader": "^4.3.0",
    "typescript": "^2.8.3",
    "ws": "latest"
  },
  "ava": {
    "require": [
      "./lib/test/helpers/setup-browser-env.js"
    ],
    "files": [
      "./lib/test/**/*.js",
      "!./lib/test/helpers/**/*.js"
    ],
    "sources": [
      "**/*.js"
    ]
  },
  "dependencies": {
    "nuid": "^1.0.0"
  }
}
